# Lista de tareas

Se marca ✅ solo cuando algo fue efectivamente probado, no solo escrito.

## Seguridad
- [x] **RLS activado en las 4 tablas** (`supabase/migrations/0002_enable_rls.sql`) — se detectó que la clave pública podía leer y escribir libremente (nombres y teléfonos incluidos) porque la migración inicial nunca activó Row Level Security. Verificado antes y después del fix con inserciones/lecturas reales: antes la clave pública leía todo, ahora devuelve vacío y rechaza inserciones (`42501`); la clave secreta y las Edge Functions siguen funcionando exactamente igual (probado con el formulario real después del cambio).

## Máquina de leads inbound — nuevo foco (20/09/2026)
- [x] **Bloque 16: renderer en la nube (GitHub Actions) + failsafe de auto_publish + modo autónomo** (20/09/2026) — con Simón ausente y autorización explícita ("modo autónomo"), se auditó el repo completo y se avanzó lo máximo posible sin acción humana. **GitHub Actions elegido sobre Railway/Fly.io** tras comparar costo real: repo público → runners Linux estándar gratis sin límite de minutos, vs. ~USD 5-15/mes de un servicio pago — ver `docs/render-pipeline-cloud.md`. Nuevo bucket público `content-assets` en Supabase Storage (RLS: solo `service_role` escribe, verificado con un intento real de escritura con la `anon key` rechazado 403) reemplaza a GitHub Pages para lo nuevo (sin commits de binarios). `scripts/cloud_render_worker.py` (nuevo) reclama piezas `render_pendiente` de forma atómica (`render_started_at`, evita doble render) y las renderiza — soporta **reel/post/story/carousel**, no solo reel. `render_carousel_asset.py` ganó 2 slides nuevas (`stat`, `comparison`) para variedad editorial real. **3 bugs reales encontrados y corregidos en el camino**: (1) `INSTAGRAM_BUSINESS_ACCOUNT_ID` nunca existía como ese nombre de secret (leía `INSTAGRAM_BUSINESS_USER_ID`) — nunca se había notado porque `auto_publish` siempre estuvo en `false`; (2) el publicador usaba `piece.cta` (corto) como caption real en vez de `piece.script`; (3) **bug sistémico**: el prompt de `content-generator` nunca aclaraba que `script` debía ser la caption publicable, y salía como nota de producción en tercera persona — corregido en el prompt (deploy hecho) y a mano en las 12 piezas ya afectadas. **Failsafe real implementado**: `consecutive_publish_failures` en `content_config` — 3 publicaciones seguidas fallidas apagan `auto_publish` solas y avisan por Telegram (antes no existía ningún mecanismo de auto-apagado). **Probado de punta a punta** con el mismo código real (`cloud_render_worker.py`): 12 piezas (2 posts, 2 carruseles nuevos + 1 carrusel previo promovido, 6 stories, 1 reel) renderizadas y subidas a Storage con éxito, quedaron en `status=listo` con caption real, URLs públicas confirmadas accesibles sin autenticación. 4 stories más quedaron en `render_pendiente`, sin tocar, específicamente para que las procese la corrida real de GitHub Actions (no procesadas a mano a propósito, para no perder la prueba headless). La corrida verdaderamente headless en GitHub Actions quedó pendiente de confirmación en el momento de escribir esto — ver el estado real en el chat/`FINAL_STATUS.md`, no acá. **Bloqueo real no resuelto**: el secret `ELEVENLABS_API_KEY` cargado a mano en GitHub no es el valor real (confirmado: la misma key local funciona perfecto contra la API real) — bloquea específicamente el render de Reels en la nube; solo Simón puede corregirlo (no hay forma de leer/escribir secrets de GitHub por API con el token disponible). `AUTO_PUBLISH` se mantuvo en `false` — no se cumple la condición explícita de Simón ("solo si TODO corre en la nube").
- [x] **Bloque 15: publicación real del Reel + primera implementación real de video vía API** (20/09/2026) — `createVideoContainer`/`waitForContainerReady`/`publishInstagramReel` implementados de verdad (antes un stub bloqueado siempre): los videos se procesan async en Meta, hay que esperar `status_code=FINISHED` antes de publicar. **Bug real encontrado**: `publishInstagramPost` usaba `piece.cta` como caption en vez de `piece.script`. Publicación real autorizada de `reel-monotributo-cobertura-final.mp4` en @simoonhaddad: media real `18097382147529920`, permalink [instagram.com/reel/DdhZLRJCmbj](https://www.instagram.com/reel/DdhZLRJCmbj/), visibilidad pública confirmada sin sesión iniciada. Ejecutado con un script de un solo uso (`scripts/publish_reel_final.py`) porque el sandbox de Claude Code bloqueó automáticamente la ejecución directa de esta acción (mutación de producción justo antes de publicar) — Simón lo corrió él mismo en su Terminal. 110 tests automáticos (7 nuevos).
- [x] **Bloque 1: captura de lead de fricción mínima** — landing rediseñada como flujo de 3 pasos (edad, cobertura actual, WhatsApp obligatorio), scoring rediseñado para medir intención en vez de cantidad de formulario completado (ver `docs/scoring.md`). 36 tests automáticos. Probado de punta a punta **tres veces**: contra la función desplegada por curl, simulando taps reales en el navegador local, y desde la URL pública de producción — en los tres casos: lead guardado con score 80/`contactar_ahora` y alerta de Telegram confirmada por el usuario. Datos de prueba borrados en cada caso.
- [x] **Bloque 2: motor de contenido V1** — tabla `content_pieces` (`supabase/migrations/0004_content_pieces.sql`) con formato, hook, guion, CTA, palabra clave, audiencia, hipótesis, fecha sugerida, estado (borrador/aprobado/publicado/descartado) y columnas de métricas para cargar después. Primera tanda real insertada y verificada por API: 3 reels, 2 carruseles, 5 stories, 2 posts = 12 piezas, todas en estado `borrador` a la espera de que Simón las revise y apruebe. Cada pieza apunta a un problema/duda real (aportes, monotributo, cartilla, mitos de precio, grupo familiar) con una palabra clave de captura (APORTES/PLAN/CARTILLA/INFO), no contenido genérico. **Bug de seguridad encontrado y corregido en el momento**: la tabla se creó sin RLS y la clave pública podía escribir filas libremente (verificado con una inserción real, código 201); se cerró con `0005_rls_content_pieces.sql` y se confirmó después que la clave pública ya no puede leer ni escribir, mientras que las Edge Functions (service role) siguen funcionando igual.
- [x] **Bloque 3: Activación V1 (atribución de leads a contenido)** — cada `content_piece` tiene ahora `landing_url` propio (`?source=instagram&content=<slug>`), `channel` y `capture_mechanism`. La landing lee `source`/`content` de la URL de forma invisible (sin agregar pasos) y los manda a `intake-landing`, que resuelve la pieza por slug y guarda `content_piece_id` + `origin_channel` en el lead (`0006_atribucion_contenido.sql`). La alerta de Telegram ahora muestra `Origen` y `Contenido` (formato + hook) + `CTA` cuando el lead viene atribuido, sin ensuciarse cuando no. Las 5 piezas de story se adaptaron para usar el link directo (mecanismo de mayor conversión, sin pasar por DM); las 7 piezas de reel/carousel/post mantienen CTA por palabra clave ya que Instagram no permite links clickeables en esos formatos — Simón comparte el `landing_url` manualmente cuando le escriben. Revisión humana: sin dashboard nuevo, se usa la tabla `content_pieces` directo en Supabase Table Editor (`docs/revision-contenido.md`), con estados borrador/aprobado/publicado/descartado y una columna `published_ref` para guardar la referencia real una vez publicada. 42 tests automáticos (2 nuevos). **Probado de punta a punta de verdad**: request real a la función desplegada con `contentSlug=reel-aportes-dependencia` → lead guardado con `content_piece_id` igual al id real de esa pieza y `origin_channel=instagram` (verificado por API) → alerta de Telegram con Origen/Contenido/CTA **confirmada por Simón**. Datos de prueba borrados después.
- [x] **Bloque 4: Motor de marketing V1** — capacidades reales de Meta verificadas contra documentación oficial (`docs/capacidades-meta.md`): publicar cualquier formato requiere `instagram_business_content_publish` + App Review, hoy no lo tenemos. Se agregó `content_config` (cadencia editable sin tocar código: reels/día, stories/día, carruseles y posts por semana, horarios, `auto_publish`, presupuesto diario de IA), `ai_usage_log` + `_shared/ai_budget.ts` (enforcement real del presupuesto antes de cualquier llamado a IA autónomo), `_shared/novelty.ts` (anti-repetición de hooks/temas/CTA), `_shared/scheduling.ts` (horarios reales en America/Argentina/Mendoza), pipeline de estados completo (`borrador→listo→programado→publicado→medido→ganador/normal/perdedor`), `parent_content_piece_id` (familias de contenido), vista `content_performance` (leads por contenido calculado en vivo desde la atribución real, no a mano). 3 funciones nuevas desplegadas y **probadas en vivo**: `content-generator` (bloqueado de forma segura sin `ANTHROPIC_API_KEY`, confirmado con cola vacía y con cola llena), `content-analyzer` (confirma que faltan datos con 0 piezas publicadas), `publish-content` (con `auto_publish=false` nunca publica, confirmado). Adapters de Instagram (`_shared/publishers/instagram.ts`) construidos y en estado `blocked` con el motivo exacto. Se generó un asset real (fondo de story 1080x1920 con degradé de marca, sin herramientas pagas) aplicado a las 14 piezas de story. Cola ampliada de 12 a **25 piezas reales** (7 reels, 14 stories, 2 carruseles, 2 posts) cubriendo genuinamente 7 días, con 2 piezas demostrando el mecanismo de "familia de contenido" (`parent_content_piece_id`). 60 tests automáticos (18 nuevos). RLS verificado en las 3 tablas nuevas. Pendiente y a cargo de Simón: decidir `ANTHROPIC_API_KEY`/presupuesto real, decidir si activar Cron de Supabase, y completar el App Review de Meta para desbloquear publicación real.
- [x] **Bloque 5: Desbloqueo + Adquisición** —
  - **Objetivo 2 (automatización 24/7)**: verificado que `pg_cron`/`pg_net` están disponibles en el plan actual (no una feature paga aparte). Activados 3 cron jobs reales en la nube: `content-generator-daily` (06:00 Mendoza), `content-analyzer-daily` (07:00 Mendoza), `publish-content-every-30-min`. Secreto interno guardado en Supabase Vault, nunca en texto plano en git. **Probado en vivo**: se disparó manualmente el mismo `net.http_post` que usa el cron y se confirmó la respuesta real (200, `waiting_approval`) guardada por Postgres.
  - **Objetivo 3 (Meta App Review)**: checklist operativo completo en `docs/checklist-app-review.md`, con los 3 videos, textos y URLs ya preparados. Se identificó (a confirmar con Simón) que "Standard Access" podría permitir publicar/leer insights en la propia cuenta SIN esperar App Review — instrucciones de 10 minutos para probarlo.
  - **Objetivo 4 (Meta Lead Ads V1)**: formulario de fricción mínima diseñado (`docs/meta-lead-ads.md`), migración `0010_meta_ads_atribucion.sql` (campaign_id/adset_id/ad_id/form_id), Edge Function `meta-leadgen-webhook` desplegada. **Bug real encontrado y corregido**: la función se desplegó primero con verificación JWT activada, lo que hubiera bloqueado silenciosamente todos los webhooks reales de Meta (que no mandan credenciales de Supabase) — se corrigió con `--no-verify-jwt`, igual que `instagram-webhook`. Probado 🟡 con un payload simulado firmado de forma independiente: firma inválida rechazada, atribución del anuncio guardada, degradación correcta sin `META_PAGE_ACCESS_TOKEN`, duplicados ignorados, alerta de Telegram con la línea "Anuncio: ..." confirmada por Simón. **Aclaración explícita (el flujo NO está completo todavía)**: sin `META_PAGE_ACCESS_TOKEN` solo funcionan recibir el evento y el `leadgen_id` (A y B) — el paso C, traer los datos reales del formulario (teléfono, edad, cobertura) vía Graph API, sigue ❌ bloqueado y nunca se probó ni simulado. Un lead real hoy se guardaría sin teléfono, solo como aviso para revisar a mano — ver detalle en `docs/meta-lead-ads.md`.
  - **Deuda técnica marcada**: hoy Simón tiene que responder manualmente por DM el link cuando alguien comenta/escribe la palabra clave en un reel/post/carrusel (Bloque 3). Se preparó el adapter bloqueado `_shared/autoresponder/instagram.ts` (`replyWithLandingLink`) para automatizar esa respuesta apenas `instagram_business_manage_messages` esté aprobado para enviar, no solo recibir.
  - 70 tests automáticos (10 nuevos). Objetivo 1 (Claude API) quedó en propuesta, sin gasto ni cuenta creada — pendiente de autorización de Simón.
- [x] **Bloque 8: token de Instagram durable + refresh automático** (20/09/2026) — causa real del error de intercambio encontrada: el token del panel YA es de larga duración (60 días), no corto; el endpoint correcto es `refresh` (`ig_refresh_token`), no `exchange` (`ig_exchange_token`). Tabla `platform_tokens` (RLS, nunca en Secrets porque necesita auto-actualizarse) + cron diario `instagram-token-refresh` (renueva con ≤30 días de margen, nunca al último día) + alerta de Telegram si falla. **Probado en vivo, dos veces**: (1) renovación real manual → token nuevo válido ~59 días con los 5 permisos intactos; (2) forzando las condiciones de refresh vía la función desplegada → renovó de verdad sola (`refresh_count` 1→2); después, insights real + creación real de contenedor (sin publicar) usando el token que el propio sistema renovó, sin intervención de Simón. Adapters (`instagram.ts`) refactorizados para leer credenciales inyectadas desde la tabla en vez de una variable de entorno estática. 84 tests automáticos (7 nuevos), incluida una prueba que escanea el repo para confirmar que ningún token real quedó en el código fuente. **No probado en vivo a propósito**: el camino de alerta de Telegram cuando el refresh FALLA (por no arriesgar el token real de producción); el código es simple y sigue el mismo patrón ya probado en otras alertas.
- [x] **Bloque 13: Reel Engine con voz real (ElevenLabs "Melanie")** (20/09/2026) — historial real probado en orden: `say` de macOS descartado (sin acento argentino) → Azure AI Speech probado con audio real (funcionó técnicamente, ~USD 0/mes, pero Simón lo escuchó y le pareció robótico) → ElevenLabs plan Free probado y **bloqueado de verdad** (`"Free users cannot use library voices via the API"`, confirmado con una llamada real) → **Simón autorizó y contrató ElevenLabs Starter (USD 6/mes)** → generadas Agustín y Melanie con el mismo guion, Simón eligió **Melanie**. `scripts/render_reel_asset.py` reescrito para sintetizar con el endpoint `with-timestamps` de ElevenLabs y armar la duración real de cada beat a partir del timing por carácter (no un número inventado) — probado de punta a punta: Reel real de 16.3s con audio y video sincronizados, mostrado a Simón. Costo real: dentro del margen de los 30.000 caracteres/mes del plan Starter a nuestra cadencia.
- [x] **Bloque 14: Reel Engine V3.1 — preset de voz definitivo (Melanie Enérgica) + música con ducking real** (20/09/2026) — a pedido de Simón (voz plana + sin ambiente en la V1 con voz), se comparó `eleven_multilingual_v2` contra `eleven_v3` con 3 perfiles reales de Melanie (Natural/Enérgica/Comercial Sutil, mismo guion) — **Simón eligió "Enérgica"**, guardado como preset único y reutilizable `MELANIE_ENERGICA` en `scripts/render_reel_asset.py` (voice_id, modelo `eleven_v3`, stability/similarity/style/speed) para no volver a comparar voces. Se agregaron audio tags no verbalizados (`[curious]`/`[upbeat]`) verificados de forma empírica: ocupan ~48ms de silencio en el timing real, no se leen en voz alta. Música de fondo real con **Eleven Music** (incluida en el plan Starter ya contratado, sin gasto nuevo — se activó el permiso `music_generation` en la API key a pedido, antes bloqueaba con 401) y mezcla real con ffmpeg: `sidechaincompress` para ducking real (la música baja sola cuando habla Melanie, no un volumen fijo), `afade` de entrada/salida y `loudnorm` para normalizar sin clipping. **Probado de punta a punta**: Reel real de 20.4s (`reel-monotributo-cobertura-final.mp4`, mismo guion/contenido que la versión anterior, sin cambios de copy) con voz+música+ducking+subtítulos sincronizados, mostrado a Simón — **sin publicar**, pendiente de su aprobación explícita. Costo exacto no verificable en tiempo real (la API key no tiene el permiso `user_read`); estimado: bien dentro del margen del plan Starter a esta cadencia.
- [x] **Bloque 12: Reel Engine V3** (20/09/2026) — reescrito de 3 escenas estáticas a beats de 1-3s con composición distinta cada uno (pregunta/stat con barra resaltadora/línea/punch/CTA), entradas tipo pop, acento circular con blur que cambia de posición, safe areas reales (top/bottom/right) para no tapar hook/CTA con la UI nativa de Reels. Bug real corregido: el resaltado de palabra clave no funcionaba si no coincidía con el final exacto de línea tras el wrap. `variant_style` agregado (`0018_variant_style.sql`) para comparar la misma idea en distintos ángulos. Primer Reel V3 real (`reel-monotributo-cobertura`, variante "pregunta", ~12.8s) — mostrado a Simón, sin publicar. **Audio investigado, no contratado**: `say` de macOS descartado (sin acento argentino); recomendado Azure AI Speech (`es-AR-ElenaNeural`/`es-AR-TomasNeural`, USD 16/millón de caracteres, 500K gratis/mes, costo real esperado ≈ USD 0/mes a nuestra cadencia) — pendiente de autorización de Simón para crear la cuenta. Música: sin descargar nada de terceros; camino recomendado es generar un fondo simple programáticamente. Carrusel verificado (identidad, legibilidad, CTA, las 6 slides linkeadas y accesibles) sin rehacer nada. `AUTO_PUBLISH` se mantuvo en `false`; no se publicó nada.
- [x] **Bloque 11: Content Rendering Engine V2 + Feedback Loop real** (20/09/2026) —
  - **Robustez del publicador**: `publishContainerWithRetry` (backoff 1s/2s/..., motivado por el HTTP 400 transitorio de la primera publicación real) — probado con fetch simulado que falla y después funciona. Idempotencia real: `pending_container_id`/`last_publish_error`/`publish_attempts` (`0013_publish_robustness.sql`) — si el contenedor ya se creó en un intento previo, se reutiliza en vez de crear uno nuevo. Telegram avisa si falla después de los reintentos. **No probado en vivo a propósito** (no se fuerza una falla real contra la cuenta de producción).
  - **Reels (prioridad máxima)**: generador real de video (`scripts/render_reel_asset.py`) — 1080x1920, H.264, con fondo de paneo/zoom continuo (no un PowerPoint) y animaciones por escena (fade/slide/scale). ffmpeg instalado gratis vía pip (`imageio-ffmpeg`, sin API paga). Primer Reel real generado a partir de `reel-aportes-dependencia` (~12s, sin audio — limitación V1 documentada), vinculado a su content_piece (`video_ref`). Sin publicar.
  - **Carruseles**: esquema extendido (`carousel_assets jsonb`, `0014_carousel_video_assets.sql`). Generador real (`scripts/render_carousel_asset.py`) — primer carrusel real de 6 slides a partir de `carousel-obra-social-vs-prepaga` (portada + 4 desarrollo + CTA), identidad Federada. Sin publicar.
  - **Stories corregidas**: verificado que Meta no permite link stickers vía API — se corrigieron las 16 stories existentes (CTA y `capture_mechanism`) para pedir respuesta por DM en vez de prometer un link que no puede existir si se publican por API; documentado que si Simón las publica a mano sí puede usar el link sticker real. Prompt del generador actualizado para no repetir el error.
  - **Analytics real**: `content-metrics-sync` (nueva función) lee insights reales en checkpoints (2h/24h/72h desde `published_at`, sin polling constante — `_shared/metrics_checkpoint_policy.ts`, bug real encontrado y corregido por un test: "nunca medido" se trataba como "ya medido para siempre"). **Probado en vivo**: con el checkpoint todavía no vencido devolvió `none`; forzando el checkpoint, trajo insights reales de la publicación (reach 10, 1 like reales) y los guardó. `content_performance` ahora expone también reach/likes/comments/saves/shares; `content-analyzer` calcula `leads_por_reach` solo cuando `reach > 0` (nunca inventado). Cron cada 2hs agregado.
  - **Feedback loop**: confirmado que con 1 sola pieza publicada, `content-analyzer` sigue rehusándose a sacar conclusiones (`1/3 mínimo`) — el guardrail contra sobreajustar a una sola publicación sigue funcionando con datos reales, no solo en teoría.
  - **published_media_id** agregado (`0016_published_media_id.sql`) — antes solo se guardaba el permalink, y hacía falta el id real para pedir insights; backfillado en la pieza ya publicada. `publish-content` ahora también resuelve el permalink real después de publicar (antes solo guardaba el media_id crudo).
  - 97 tests automáticos (16 nuevos). App Review sigue pendiente y de alta prioridad (sin avanzar en este bloque, según lo pedido). `AUTO_PUBLISH` se mantuvo en `false` en todo momento; no se publicó ninguna pieza nueva.
- [x] **Bloque 10: PRIMERA PUBLICACIÓN REAL EN INSTAGRAM** (20/09/2026) — con autorización explícita de Simón, se publicó `post-error-frecuente-cartilla` vía API oficial (contenedor real → `media_publish` real). **Circuito completo validado de punta a punta**: content_piece → asset → Instagram real → publicación → insights → base de datos. Media real `18185981965409625`, permalink [instagram.com/p/Ddg2QaxlFgK](https://www.instagram.com/p/Ddg2QaxlFgK/), **visibilidad pública confirmada con el navegador sin sesión iniciada** (se ve el perfil real de @simoonhaddad y el botón "Escribime CARTILLA"). `content_pieces` actualizado: `status=publicado`, `published_ref` (permalink real), `published_at` (timestamp real de Meta, nueva columna `0012_published_at.sql`), e insights reales guardados (reach/saves/likes/comments/shares en 0, esperable recién publicado — `impressions` no se guardó porque Meta no soporta esa métrica para este tipo de publicación, no se inventó). Verificado que `content_performance` ya vincula la publicación con su `content_piece_id` para contar leads futuros. `AUTO_PUBLISH` se mantuvo en `false` durante todo el proceso — la publicación fue 100% manual/autorizada, no automática. No se publicó ninguna otra pieza.
- [x] **Bloque 9: verificación oficial de link stickers en Stories + identidad visual real** (20/09/2026) — Simón pidió verificar contra documentación oficial si la API permite agregar un link sticker a una Story; la referencia `ig-user/media` de Meta dice explícitamente **"Publishing stickers (i.e., link, poll, location) is not supported"** — se descartó `story-faq-aportes-prepaga` como primera prueba porque su CTA dependía de eso. Se detectó además que ningún post/carrusel/reel tenía asset generado (solo las stories). Se generó el asset correcto para post (1080×1350, dentro del rango que acepta el feed) y luego, a pedido de Simón, se **rehizo con la identidad visual real de Federada** (antes era un degradé verde genérico): tipografías Red Hat Display/Text (descargadas de Google Fonts, licencia OFL, guardadas en `assets/fonts/`), azul institucional `#001489` como base, magenta `#F04E98` como acento estratégico, botón de CTA real con flecha dibujada a mano (la fuente no traía el glifo). Regla de identidad visual documentada (`docs/identidad-visual-federada.md`) como estándar obligatorio para toda pieza futura. Script reutilizable `scripts/render_post_asset.py` (Pillow). Pieza candidata para la primera publicación controlada: `post-error-frecuente-cartilla` (`content_piece_id 74af9745-1cea-4d3f-ab86-3117ce5b8c69`), con hook/CTA/caption reescritos para coincidir con el nuevo diseño — **todavía sin publicar, esperando autorización explícita de Simón**.
- [x] **Bloque 6: Objetivo 1 activado — Anthropic API real** (20/09/2026) — cuenta creada por Simón, USD 5 de crédito, recarga automática apagada. `ANTHROPIC_API_KEY` cargada como secret de Supabase. **Probado en vivo, en orden**: (1) enforcement del presupuesto con la key real ya configurada — se simuló gasto de USD 2.00 (el límite) y `content-generator` devolvió `blocked` sin llamar a Anthropic; (2) generación real de prueba — encontró y corrigió 2 bugs reales (Claude envolvía el JSON en \`\`\`json sin que el parser lo esperara; los slugs generados traían tildes/espacios sin sanitizar rompiendo la URL de atribución; el `keyword` no siempre coincidía con la palabra del CTA) antes de dar el bloque por bueno. Tras el fix: 3 corridas reales, 11 piezas nuevas, 0 rechazadas por las reglas de variedad, costo real total **USD 0.0276**, ~17-19s por corrida, modelo `claude-haiku-4-5-20251001`. Cola actual: 30 piezas / 8 días de buffer. El cron diario ya generará solo la próxima vez que la cola baje de 7 días — no requiere ninguna activación adicional.
- [x] **Bloque 7: Objetivo 2 — Standard Access de Instagram confirmado con prueba real** (20/09/2026) — Simón agregó `instagram_business_content_publish` e `instagram_business_manage_insights` en modo prueba y pasó un token real. Se probó (sin publicar nada visible, tal como se pidió): creación real de un contenedor de imagen (`FINISHED`, no publicado) y lectura real de insights de la cuenta (reach: 588 y 120). **Resultado: ✅ Standard Access alcanza para publicar/leer insights en la propia cuenta, sin Advanced Access/App Review.** `_shared/publishers/instagram.ts` y el nuevo `_shared/publishers/instagram_graph.ts` ahora implementan la publicación real (ya no son un stub) para `post`/`story`; `carousel`/`reel` siguen bloqueados pero **ahora por falta de assets (múltiples imágenes / video), no por permiso**. Sigue gateado por `content_config.auto_publish=false`, sin cambios de riesgo. **Nuevo blocker real encontrado**: el token de Instagram es de corta duración y el intercambio a uno de 60 días falla ("Session key invalid", error reproducible, ya visto antes) — pendiente de resolver para automatizar sin intervención manual periódica. 77 tests automáticos (7 nuevos). Documentación actualizada (`docs/capacidades-meta.md`, `docs/checklist-app-review.md`) para reflejar que el App Review completo solo sigue haciendo falta para comentarios/DMs de desconocidos, no para publicar contenido propio.
- [ ] WhatsApp Business Cloud API como canal de conversación (futuro cercano)
- [ ] Flujo guiado por botones para Instagram (máx. 2-3 preguntas) una vez que Meta apruebe el acceso

## Fase 0 — Fundaciones
- [x] Estructura del repositorio
- [x] README completo
- [x] `.env.example`
- [x] Documentación de arquitectura, cuentas y costos
- [ ] Cuenta de GitHub creada y repo subido (a cargo del usuario)

## Fase 1 — Captación propia (formulario → lead → score → alerta)
- [x] Esquema de base de datos (`supabase/migrations/0001_init.sql`)
- [x] Lógica de puntaje (`scoring.ts`) + tests automáticos
- [x] Landing page con formulario (`landing/index.html`)
- [x] Edge Function `intake-landing` (recibe el formulario, guarda el lead, calcula score)
- [x] Helper de Telegram para notificaciones
- [x] Cuenta de Supabase creada por el usuario
- [x] Bot de Telegram creado por el usuario — probado, el mensaje de prueba llegó ✅
- [x] Migración aplicada en el proyecto real de Supabase — verificado con curl contra las 4 tablas (200 OK) ✅
- [x] Edge Function desplegada en el proyecto real
- [x] Landing page publicada en GitHub Pages — https://simonhaddadfederada.github.io/federada-inbound-engine/ (probado desde la URL real, lead confirmado en la base y limpiado después)
- [x] **Prueba end-to-end real**: lead "Prueba Real" enviado a la función
      desplegada → score 105 → `contactar_ahora` → guardado en `leads`
      (verificado vía REST API) → alerta de Telegram recibida y confirmada
      por el usuario ✅ **Fase 1 funcionando de punta a punta.**

## Fase 2 — Canales de Meta
- [x] Business Manager con Página de Facebook e Instagram vinculados
- [x] App de Meta for Developers creada (`Federada Inbound Engine`, App ID `1113996954538798`)
- [x] Permisos de Instagram agregados (`instagram_business_basic`, `manage_comments`, `manage_messages`)
- [x] Cuenta de Instagram agregada y aceptada como tester
- [x] Webhook de Instagram (comentarios + DM, detección de palabra clave) — código escrito, 15 tests automáticos (firma HMAC + parseo de eventos), desplegado y **probado con un webhook simulado firmado de verdad**: guardó el lead y mandó la alerta de Telegram
- [x] Webhook verificado y registrado en Meta (handshake ✓, campos `comments` y `messages` suscritos)
- [ ] **Bloqueado por Meta, no por nosotros**: la app está "Sin publicar" y necesita pasar el **App Review** de Meta (2-4 semanas) para recibir eventos reales de personas que no sean testers — ver [`docs/setup-fase-2.md`](docs/setup-fase-2.md). Confirmado con una prueba real: un comentario desde otra cuenta no generó ningún evento, tal como advierte Meta.
- [x] Política de privacidad e instrucciones de eliminación de datos publicadas (`privacidad.html`) y cargadas en la configuración de la app
- [x] Solicitud de App Review creada (`submission_id 1114055364532957`), textos de "Uso permitido" y "Gestión de datos" completos
- [ ] Verificación del negocio en Business Manager — **a cargo del usuario**, necesita datos legales reales
- [ ] Grabar los 3 videos de demostración (uno por permiso) — **a cargo del usuario**, instrucciones exactas en `docs/setup-fase-2.md`
- [ ] Cargar los videos y enviar la solicitud de App Review
- [ ] Webhook de Facebook Messenger
- [ ] Webhook de Meta Lead Ads
- [ ] WhatsApp Business Cloud API conectado
- [ ] Flujo de preguntas por botones/quick-replies (sin IA) para estos canales
- [ ] Clasificador liviano con IA solo para mensajes de texto libre ambiguo

## Fase 3 — Motor de contenido
- [ ] Cron semanal de generación de ideas de contenido (batch, con IA)
- [ ] Plantilla de cada idea: formato, copy, CTA medible
- [ ] Panel/tabla de revisión y aprobación humana antes de publicar
- [ ] Integración de API de Anthropic con tope de gasto diario

## Fase 4 — Analítica y optimización
- [ ] Tabla de métricas por pieza de contenido
- [ ] Carga de métricas (manual al inicio, luego vía API de Meta Insights)
- [ ] Cron diario: resumen de qué funcionó / qué no / qué probar
- [ ] Cron semanal: análisis estratégico mayor
- [ ] Priorización de leads calificados y ventas por encima de likes

## Fase 5 — Automatización ampliada
- [ ] Feature flags por módulo (auto-responder, auto-publicar, etc.)
- [ ] Aprobación humana configurable por módulo
- [ ] Panel de control simple para activar/desactivar módulos
